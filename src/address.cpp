#include "address.h"

Address::Address(std::string street, std::string postal_code, std::string city, std::string district, std::string country):
                 street(street), postal_code(postal_code), city(city), district(district), country(country){}
